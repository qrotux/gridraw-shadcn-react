// Shared Tailwind classes for the native form controls (the <select> elements
// used across the grid), so the shadcn form-control look lives in one place
// instead of being repeated at every call site.
export const controlClass = "h-8 rounded-md border border-input bg-transparent px-2 text-sm shadow-sm";
